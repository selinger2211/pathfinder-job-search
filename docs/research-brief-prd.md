# Research Brief Module — Standalone PRD

**Parent:** Pathfinder Job Search System
**Module:** `modules/research-brief/`
**Version:** v3.13
**Last Updated:** 2026-03-13
**Status:** Active — v3.13.0 features live

---

## 1. Purpose

The Research Brief is the preparation engine of Pathfinder. Before every interview loop, networking call, or application decision, you should be able to open a brief and know: what this role actually needs, what this company is doing right now, who you'd work with, how your experience maps, and exactly what to say.

**v1 was a UI shell with template strings.** v2 is a ground-up rebuild where every section is generated by Claude in the browser via direct API calls, fed with real data (JD text, company profile, your resume bullets, your connections), and sourced with citations per Section 7.12 of the main PRD.

### Design Principles

1. **JD is the anchor.** Every section reads the job description. Nothing generates in a vacuum. If there's no JD, the brief says so and asks you to add one.
2. **Your experience is the lens.** The brief isn't a Wikipedia article about the company — it's a preparation document for *you* interviewing for *this role*. Your resume bullets, story bank, and connections feed into every relevant section.
3. **Trust but Verify.** Every claim is cited. Every source is timestamped. Stale data is flagged. The user can click through to the original source. (See main PRD Section 7.12.)
4. **Browser-first generation.** Generation happens client-side via direct Claude API calls from the browser. Prompt templates are embedded in the module code. Results are cached locally (localStorage) and optionally persisted via an HTTP bridge (`localhost:3456`) for durability. The browser works fully offline if the bridge is unavailable.

---

## 2. Architecture

### Generation Flow

```
User selects role from dropdown (or deep-links via ?roleId=xyz)
        │
        ▼
Browser checks localStorage cache:
  research_{companySlug}_{roleId}_{sectionNum}
        │
        ├─── Cache hit? ──→ Render cached section
        │
        └─── Cache miss or user clicks "Clear & Regenerate"?
              │
              ▼
          Browser calls generateBrief() JavaScript function
                │
                ▼
          Sections generate sequentially (batches: pre, 1, 2, 3)
                │
                ├── Batch 1: Sections 1-6 (independent)
                │   All parallel via Promise.allSettled()
                │
                ├── Batch 2: Sections 7-10 (depend on batch 1 output)
                │   All parallel
                │
                └── Batch 3: Sections 11-13 (depend on batch 2 output)
                      All parallel
                │
                ▼
          For each section:
          - Browser calls PF.claude.generate() (direct Claude API)
          - System prompt + context block + section-specific prompt
          - Claude returns HTML + citations JSON
          - Result cached in localStorage
          - Bridge endpoint (optional): POST to localhost:3456/persist
            (if bridge available, persists to database)
          - Section rendered in UI with "Fresh" badge
                │
                ▼
          After all sections complete:
          - Auto-save brief to localStorage
          - Attach brief reference to pipeline role
          - Generate PDF (html2pdf) → store in IndexedDB
          - Update "Generate" button to "Regenerate"
```

**Fallback Mode:** If the HTTP bridge (`localhost:3456`) is unavailable or returns an error, the browser automatically falls back to localStorage-only mode. The user never sees a "bridge error" — generation continues seamlessly.

### Data Inputs Per Section

Every section receives a **base context block** (assembled by `gatherBriefContext()`) containing:

- **JD text** — the full job description from `role.jdText`, wrapped in `<jd>` tags
- **Role metadata** — title, level, positioning (IC/management), stage, URL, dateAdded, salary hints
- **Company profile** — name, domain, fundingStage, headcount, missionStatement, companyType, remotePolicy, techStack
- **Role hints** (when JD is absent) — partial intel from `role.roleHints`: function, level, scope, teamSize, productArea, techStack, reportingLine, and any freeform notes
- **Known context log** — timestamped entries from `role.knownContext[]`, each with source and note

Additionally, sections that reference **connections, resume bullets, stories, or compensation data** receive:

- **Connections** — from `pf_connections[]`, filtered to match the company, wrapped in `<connections>` tags
- **Bullet bank** — from `pf_bullet_bank[]`, wrapped in `<bullet_bank>` tags
- **Story bank** — from `pf_story_bank[]`, wrapped in `<story_bank>` tags
- **Comp data** — from `pf_comp_cache`, wrapped in `<comp_data>` tags
- **Interviewer names** — if provided in the sidebar, wrapped in `<interviewer_names>` tags
- **Previously generated sections** — output from earlier batches, wrapped in `<previously_generated_sections>` tags for dependent sections to reference

If a required input is missing, the section generates anyway and appends a visible callout: "This section would be stronger with: [missing data]. Add them in Pipeline → Role Detail."

**When there's no JD:** Sections generate from `role.roleHints` and `role.knownContext` instead, with clear labeling: "Based on recruiter intel — will be updated when JD is available."

**Company mismatch guard:** The system checks if the JD's first 500 characters mention the company name. If not, it prepends a warning to the JD: "This JD may have been incorrectly matched to this company. ALWAYS use [Company] as the company — ignore any other company names in the JD." This prevents Apify-scraper errors from causing the AI to confidently write about the wrong company.

---

## 2.5 Implemented Features

The following features are **fully built and shipped** in the Research Brief module:

### Core Generation
- **Browser-first architecture** — Direct Claude API calls from JavaScript, no MCP dependency
- **Embedded prompt templates** — 14 section-specific prompts defined in `SECTION_PROMPTS` object
- **Sequential batch generation** — Batches 1, 2, 3 run in series; sections within each batch run in parallel
- **localStorage caching** — Results stored with keys: `research_{companySlug}_{roleId}_{sectionNum}`
- **HTTP bridge fallback** — Optional `localhost:3456` bridge for persistent storage; app works fully offline if unavailable

### UI & Navigation
- **Role selector dropdown** — Groups roles by pipeline stage (Hot, Active, Watching, etc.)
- **Sidebar table of contents** — Section list acts as navigation; click to scroll to section
- **Deep-linking** — URL parameter `?roleId=xyz` pre-selects role on page load
- **Section keyboard shortcuts** — Keys 0-9 jump to sections 0-9; Shift+0-3 jump to sections 10-13
- **Sidebar collapse toggle** — Hide/show left context panel on mobile/narrow screens

### Data & Persistence
- **PDF generation & storage** — `generateAndStoreBriefPDF()` creates PDF via html2pdf, stores in IndexedDB
- **Brief attachment to role** — Brief metadata stored in pipeline role's `artifacts[]` array for discovery from Pipeline module
- **Auto-generation on first visit** — When navigating to a role with no cached brief, generation starts automatically
- **Clear & Regenerate button** — Clears cache and auto-triggers fresh generation (no empty state)

### AI & Citations
- **System prompt with guardrails** — Declares company from `<company>` tag as authoritative; warns about company mismatch in JD
- **Citation parsing** — Claude returns HTML + JSON `<citations>` block; system extracts and stores
- **Missing inputs detection** — System tracks which data inputs were available vs. missing (JD, bullets, stories, connections, comp data)
- **Missing inputs callout** — Section includes visible callout: "This section would be stronger with: [X, Y, Z]"

### Error Handling
- **API key prompts** — User prompted for Anthropic API key on first use; stored in localStorage (masked in UI)
- **Bridge health check** — `checkBridgeHealth()` pings bridge; sets fallback flag if unavailable
- **Per-section error UI** — Failed section shows error message + "Retry" button
- **Automatic fallback** — If bridge call fails, automatically falls back to direct API without user intervention

---

## 3. Section Specifications

### Section 0: Known Context (Appears Only When JD Is Absent)

**Purpose:** When you don't have a JD yet, this section is the anchor instead. It aggregates everything you *do* know about the role from recruiter conversations, emails, and your own research into a structured format that the other sections can use.

**Inputs:** `role.roleHints` + `role.knownContext[]` + `role.recruiterSource`

**When this section appears:** Only when `role.jdText` is empty or `role.confidential.role === true`. When a full JD exists, this section is hidden — the JD itself serves as the anchor.

**Output structure:**
- **Source of This Role** — Who told you about it, when, through what channel. "Sarah Chen (Greylock) reached out via LinkedIn on March 5. Initial message: 'I'm working with a portfolio company on a product leadership search.'"
- **What We Know** — Structured summary of all `roleHints` fields that have values:

```
Function:       Product Management
Level:          Staff / Senior Staff
Scope:          "New product area, 0-to-1"
Product Area:   Payments platform
Team Size:      "Building a new team, ~5-8 to start"
Reporting Line: "Reports to VP Product"
Tech Stack:     "Python, React, heavy ML/data"
Location:       "SF preferred, open to remote"
```

- **Recruiter Intel Log** — Chronological entries from `role.knownContext[]`, each with a date, source (call, email, message), and what was learned. This is the running log of everything the recruiter has disclosed:

```
Mar 5 — LinkedIn message from Sarah Chen
  "Product leadership role at a portfolio company, Series D, AI space"

Mar 7 — Phone call with Sarah Chen (30 min)
  "It's Stripe. Payments platform team. They're building a new
   sub-team focused on AI-powered fraud detection. Staff PM level.
   Reports to VP Product. Team is 0-to-1, they want someone who
   can define the roadmap. Interview process is 5 rounds."

Mar 9 — Email from Sarah Chen
  "JD is being finalized, should have it next week. In the meantime
   they'd like to do an intro call with the hiring manager."
```

- **Reconstructed Role Profile** — Based on all the above, a synthesized paragraph: "This appears to be a Staff PM role on Stripe's payments platform, focused on building a new AI-powered fraud detection product from scratch. The team is being formed (~5-8 people) and you'd report to the VP of Product. The role signals high ownership, ambiguity tolerance, and AI/ML product experience." This paragraph is what the downstream sections use as a JD substitute.
- **What's Still Unknown** — Explicit list of gaps: "Full JD, explicit requirements, compensation range, interview panel names." Each gap shows which brief sections it would unlock.

**Prompt guidance:** This section is primarily data assembly, not generation. The model structures the user's known context entries and role hints into a clean format. The "Reconstructed Role Profile" paragraph is the only generative part — it synthesizes the hints into a coherent role description. This paragraph should be conservative: only assert what the data supports, flag anything uncertain.

**Citations:** Every intel entry sourced to `role.knownContext[]` with date and channel. Source type: `manual_entry`. The reconstructed profile is `ai_generated` with input summary.

---

### Section 1: Role Decode

**Purpose:** Parse the JD and surface what this role actually needs — the stuff between the lines that a recruiter won't say.

**When JD is absent:** This section generates from the Known Context section's "Reconstructed Role Profile" instead of a real JD. Output is clearly labeled: "⚠ Based on recruiter intel — will be updated when JD is available." The analysis is less precise but still covers: inferred requirements from role hints, level signals from the recruiter's language, and red flags or watch items. The explicit/implied requirements split may be less clear, but the model should still attempt it based on what's known.

**Inputs:** Base context (JD is critical)

**Output structure:**
- **The Real Problem** — One paragraph: what business problem is this hire solving? Inferred from JD language, team description, and company stage.
- **Explicit Requirements** — Bulleted list of what the JD directly asks for (years of experience, specific skills, domain knowledge). Each tagged as `must-have` or `nice-to-have` based on JD language ("required" vs "preferred", "bonus", "ideally").
- **Implied Requirements** — What the JD signals without stating. Examples: "comfortable with ambiguity" → early-stage product, undefined roadmap. "Cross-functional leadership" → no direct reports, influence-based role. "0-to-1" → greenfield, expect to define the problem.
- **Level Signals** — What seniority indicators are in the JD? Scope of ownership, decision-making authority, team size mentioned, reporting line hints. How does this match the stated title?
- **Red Flags & Watch Items** — Anything unusual: unrealistic scope for the level, conflicting requirements, buzzword density suggesting the role isn't well-defined yet.

**Prompt guidance:** The model should read the JD as a hiring manager would write it — with constraints, urgency signals, and organizational context embedded in word choices. This is not a summary; it's an interpretation.

**Citations:** Each requirement traced to the specific JD line(s) it comes from. Source type: `job_board` if imported from URL, `manual_entry` if pasted.

---

### Section 2: Company Now

**Purpose:** What's happening at this company *right now* that's relevant to *this role*. Not a founding story — a situational briefing.

**Inputs:** Base context + `company.recentNews` (if populated) + web enrichment data

**Output structure:**
- **Current Moment** — 2-3 paragraphs on what the company is doing this quarter. Product launches, strategic shifts, market moves, leadership changes. Everything timestamped.
- **Why This Role, Why Now** — Hypothesis: what's driving this hire? New product line? Team scaling? Backfill? Derived from company context + JD language.
- **Key Metrics** (if available) — Revenue signals, growth rate, user base, market share. For public companies: recent earnings highlights. For private: last known fundraise, headcount trajectory.
- **Leadership** — CEO, relevant VP/SVP over this function, CTO (if engineering-adjacent role). One line each: name, tenure, background, notable decisions.

**Prompt guidance:** Prioritize recency. A company overview from 2024 is stale. If the model's training data is limited, it should say so and flag sections for manual research. Never fabricate news or metrics.

**Citations:** Every fact sourced. News articles, earnings calls, press releases, LinkedIn profiles. Source type: `enrichment_web` with URLs and fetch dates.

---

### Section 3: Funding & Corporate Structure

**Purpose:** Follow the money. Who funds this company, what's the cap table story, and what's the corporate family tree.

**Inputs:** Base context + `company.fundingStage` + web enrichment data

**Output structure:**
- **Funding History** — Table: Round, Date, Amount, Lead Investor(s), Valuation (if known). Most recent round first.
- **Notable Investors & Board Members** — Names, firms, and why they matter (e.g., "Sequoia led Series B — their portfolio includes [relevant competitors]"). Board composition signals (heavy on operators vs. financial investors).
- **Corporate Structure** — Parent company (if subsidiary), subsidiaries, recent acquisitions, joint ventures. For conglomerates: where does this business unit sit?
- **Financial Health Signals** — For public companies: market cap, P/E, revenue trend. For private: runway signals (last raise date vs. headcount growth), any known profitability milestones. For both: any recent layoffs, hiring freezes, or aggressive hiring signals.
- **What This Means for You** — One paragraph connecting the financial picture to the role. Example: "Series D with 18 months since last raise and aggressive hiring suggests strong revenue growth or an upcoming raise. Equity could be valuable."

**Prompt guidance:** Use known funding databases (Crunchbase, PitchBook-style data). If the model doesn't have specific data, it should say "Not found in available data — verify on Crunchbase" rather than guessing. Never fabricate funding amounts or valuations.

**Citations:** Each funding round sourced. Investor names sourced. Source type: `enrichment_web`.

---

### Section 4: Competitive Landscape

**Purpose:** Who is this company fighting, specifically in the product area this role touches.

**Inputs:** Base context (JD text is critical for scoping the product area)

**Output structure:**
- **Direct Competitors** — 3-5 companies competing in the same product space as this role. For each: one-line description, estimated scale, key differentiator vs. the target company.
- **Indirect / Adjacent Competitors** — 2-3 companies that compete tangentially or could expand into this space.
- **Competitive Dynamics** — What's the market narrative? Is it consolidating? Land-grab phase? Feature parity race? Who's winning and why?
- **Target Company's Moat** — What's their defensible advantage? Network effects, data, distribution, brand, switching costs, regulatory?
- **Interview Leverage** — How to reference the competitive landscape in interviews without sounding like you're reading a report. One example talking point.

**Prompt guidance:** Scope competitors to the *product area the role sits in*, not the company overall. A PM role on Stripe's billing product competes with Chargebee and Recurly, not with Square's POS hardware. The JD should provide scoping signals.

**Citations:** Competitor claims sourced. Market data sourced. Source type: `enrichment_web` or `ai_generated` (with explicit "based on model knowledge as of [date]" flag).

---

### Section 5: Team & Org Intelligence

**Purpose:** Who would you work with? Who's the hiring manager? What does the org look like?

**Inputs:** Base context + `role.interviewerNames` (if provided) + `pf_connections` (for cross-reference)

**Output structure:**
- **Likely Reporting Line** — Inferred from JD: who does this role report to? VP Product? Director? Group PM? What does that imply about scope and visibility?
- **Team Shape** — Estimated team size, composition (PMs, engineers, designers, data), and org model (squad, pod, functional) — inferred from JD language.
- **Adjacent Teams** — Who would this role interact with most? Sales, marketing, engineering leadership, customer success? Based on JD's "cross-functional" mentions.
- **Interviewer Profiles** (if names provided) — For each interviewer: name, likely title (from LinkedIn if known), estimated tenure, background summary, what they probably care about in this interview. If interviewer names are not provided, show a prompt: "Add interviewer names in the sidebar to unlock this section."
- **Org Observations** — Any signals about the org from the JD or public info: new team (no established process), re-org (role reporting line changed), rapid growth (junior team needs senior leadership).

**Prompt guidance:** Interviewer research should be cautious — use only publicly available professional information (LinkedIn, company team pages, conference talks). Never fabricate personal details. Flag uncertainty: "Based on LinkedIn profile from [date], may have changed."

**Citations:** Interviewer data sourced to LinkedIn profiles or company pages. Org structure claims sourced to JD lines.

---

### Section 6: Network & Connections

**Purpose:** Who do you already know at this company? Who can make an intro? Turn your network into an advantage.

**Inputs:** Base context + `pf_connections` (full connections list) + `pf_roles` (for company match)

**Output structure:**
- **Direct Connections** — People in your `pf_connections` who work at this company. For each: name, title, relationship context (how you know them), last interaction date, and suggested action (warm intro request, back-channel reference, informational chat).
- **Second-Degree Paths** — Connections who previously worked there, or who are connected to known employees. "You know [Name] at [Other Company] who previously worked at [Target Company] as [Title]."
- **Suggested Outreach** — For each viable connection, a one-liner on what to say. Not a full email draft (that's the Outreach module) but a hook: "Mention your shared time at [Event/Company]. Ask about the [Team] culture."
- **No Connections?** — If no matches found: "No connections found at [Company]. Consider: LinkedIn search for [Company] + [your school/previous employers], checking if any pipeline contacts have moved there recently."

**Prompt guidance:** This section is data-driven, not generative. The model cross-references `pf_connections` against the company name. Creative suggestions for second-degree paths are welcome, but the core list must be factual from the user's data.

**Citations:** Each connection sourced to `pf_connections` data. Source type: `manual_entry` (user-entered connections).

---

### Section 7: Fit Analysis

**Purpose:** The honest answer to "why should they hire you?" — mapped requirement by requirement against your actual experience.

**Inputs:** Base context + `pf_bullet_bank` + `pf_resume_log` (most recent base resume) + `pf_story_bank`

**Output structure:**
- **Requirements Map** — Table with columns: Requirement (from JD), Your Evidence (matched bullet or story), Fit (green ✅ / yellow ⚠️ / red ❌), Notes.
  - **Green:** You have direct, demonstrable experience. Matched to a specific bullet or story.
  - **Yellow:** You have adjacent experience that can be positioned. Notes explain the positioning angle.
  - **Red:** Genuine gap. No relevant experience found in your data.
- **Overall Fit Score** — X/Y requirements matched (green + yellow count). Not a percentage — a concrete count so you know the ratio.
- **Gap Positioning Strategies** — For each yellow/red requirement: how to talk about it without lying. Example: "No direct fintech experience, but your payment integration work at [Previous Company] demonstrates the analytical rigor. Frame it as: 'I've built the technical intuition from the integration side and I'm eager to go deeper on the product side.'"
- **Secret Weapons** — Experience from your bullet bank that the JD doesn't ask for but would be valuable. The "and I also bring X" talking points.

**Prompt guidance:** This must be honest. The model should never fabricate matches or inflate yellow to green. A brief that tells you "you're perfect" is useless — a brief that tells you "here are your 2 gaps and here's how to talk about them" is preparation gold. If `pf_bullet_bank` is empty, flag it prominently and generate a degraded version using only JD analysis.

**Citations:** Each matched bullet sourced to `pf_bullet_bank` entry. Each requirement sourced to JD line. Source type: `manual_entry` (your bullets) + `job_board` (JD requirements).

---

### Section 8: Compensation Intelligence

**Purpose:** Know your number before they ask. Understand the comp structure so you negotiate from data, not guesses.

**Inputs:** Base context + `pf_comp_cache` (from Comp Intelligence module) + JD salary info (if present)

**Output structure:**
- **Expected Range** — Base salary range for this title + level + location + company stage. Source: Levels.fyi, Glassdoor, JD-stated range, or comp cache data.
- **Total Comp Structure** — How this company typically pays: base + bonus + equity. RSUs vs. options. Vesting schedule (4-year with 1-year cliff is standard; note deviations). Refresh grant norms.
- **JD Comp Signals** — Does the JD state a range? Is it broad (suggests flexibility) or tight? Is the range competitive for the market? Does the JD mention "competitive compensation" (often means below-market base, above-market equity)?
- **Negotiation Leverage Points** — Signals from the JD and company context that suggest leverage: urgency language, niche skill requirements, team growth signals, scarce talent market.
- **Your Target** — Based on the data, a suggested ask range (low / target / stretch) with rationale. Caveat: "This is a data-informed starting point, not financial advice."

**Prompt guidance:** Use levels.fyi and glassdoor-style knowledge. Never fabricate specific salary numbers — always cite the source and date. If no data available, say so and suggest where to look. Flag if the JD-stated range seems misaligned with market data.

**Citations:** Comp data sourced to levels.fyi, Glassdoor, JD text, or `pf_comp_cache`. Source type varies.

---

### Section 9: Strategic Challenges & First 90 Days

**Purpose:** Walk into the interview with a hypothesis. "Here's what I think you're facing and here's what I'd do about it."

**Inputs:** Base context (JD + company profile critical)

**Output structure:**
- **Top 3 Strategic Challenges** — Derived from JD priorities, company stage, and competitive landscape. For each: the challenge (one sentence), why it matters now (one paragraph), and evidence from the JD or company context.
- **Your First 90 Days** — A realistic, phase-based plan:
  - **Days 1-30:** Listen and learn. What would you try to understand? Who would you talk to? What data would you look at?
  - **Days 31-60:** Quick wins. What could you ship or unblock that demonstrates impact without requiring deep institutional knowledge?
  - **Days 61-90:** Strategic bets. What would you propose to the team/leadership? Based on which of the challenges above?
- **Talking Points** — 2-3 specific things you could say in the interview to demonstrate strategic thinking about *this* role at *this* company. Not generic frameworks — specific hypotheses.

**Prompt guidance:** The 90-day plan should be realistic for the level. An IC PM's first 90 days look different from a VP's. Avoid generic "build relationships and learn the product" — that's assumed. Focus on specifics derived from the JD and company context. It's OK to say "I'd validate whether X is true" — showing you have hypotheses is the point.

**Citations:** Each challenge sourced to JD language or company context. Source type: `job_board` + `enrichment_web`.

---

### Section 10: Culture & Values Decode

**Purpose:** Understand what this company *actually* values — not just what the careers page says.

**Inputs:** Base context + `company.culture` field + `company.glassdoor` (if available)

**Output structure:**
- **Stated Values** — What the company says it values (from careers page, mission statement, leadership quotes). Listed with source.
- **Signals from the JD** — What the JD language implies about culture. "Fast-paced" = long hours? "Collaborative" = consensus-driven? "Entrepreneurial" = under-resourced? Decoded, not cynically — but realistically.
- **Interview Style Signals** — What to expect: case study? System design? Behavioral? Portfolio review? Panel vs. 1:1? Inferred from company size, stage, and industry norms. If interviewer names provided, may refine based on their likely role in the loop.
- **What Gets Rewarded** — Based on company stage and JD language: is this a company that rewards shipping fast, or careful analysis? Builder or optimizer? Consensus or conviction?
- **Culture Fit Questions for You** — 3-4 questions you should ask yourself: "Am I comfortable with [X level of ambiguity]?" "Do I thrive in [consensus/conviction] cultures?" Based on the specific signals found.

**Prompt guidance:** Be nuanced. "Fast-paced" doesn't always mean burnout. "Process-oriented" doesn't always mean slow. Read the signals in context of company stage, size, and industry. Glassdoor data is noisy — weight recent reviews more heavily and note sample size.

**Citations:** Values sourced to careers page or mission statement. JD language sourced to specific lines. Glassdoor data timestamped.

---

### Section 11: Questions to Ask

**Purpose:** Role-specific questions organized by interview round that demonstrate you've done the homework.

**Inputs:** Base context + all previously generated sections (this section should run last or near-last)

**Output structure:**
- **Recruiter Screen** (3-4 questions) — Logistics, process, timeline, team structure validation. Things you need to know to prepare for the next round.
- **Hiring Manager** (4-5 questions) — Role-specific: what does success look like in 6 months? What's the team's biggest challenge right now? What happened to the last person in this role? Questions that demonstrate strategic thinking.
- **Technical / Panel** (3-4 questions) — Product/engineering-focused: tech stack decisions, product process, how they handle X (specific to JD requirements). Questions that demonstrate depth.
- **Executive / Final Round** (3-4 questions) — Company direction, team investment, strategic priorities. Questions that show you're thinking about the big picture.
- **Back-pocket Questions** — 2-3 questions that work in any round, if you run out or the conversation goes sideways.

**Prompt guidance:** Every question should be derivable from the brief's other sections. "I noticed in the JD that you mentioned [X] — how does that relate to [company's recent Y]?" is a good question because it proves you read the JD and know the company context. Generic questions ("tell me about the culture") should never appear here.

**Citations:** Each question linked to the brief section(s) that informed it. Source type: `ai_generated` with inputSummary listing the upstream sections.

---

### Section 12: TMAY Script

**Purpose:** A "Tell Me About Yourself" narrative built from your actual resume, positioned for this specific role.

**Inputs:** Base context + `pf_bullet_bank` + `pf_resume_log` (most recent) + Section 1 (Role Decode) output

**Output structure:**
- **90-Second Version** — The tight version for recruiter screens and time-constrained intros. Arc: where you've been → what you're great at → why this role.
- **2-Minute Version** — The expanded version for hiring manager and panel rounds. Same arc with more detail: specific accomplishments, named companies, quantified impact, explicit connection to the role.
- **Key Beats** — The 4-5 inflection points in your career that this script hits. For each: why it's included (maps to which JD requirement) and what to emphasize.
- **What to Skip** — Experience from your resume that you should *not* bring up unprompted for this role (irrelevant to the JD, or could raise questions without adding value).
- **Transition Line** — How to end the TMAY and hand control back: a bridge sentence that connects your story to the role and invites the next question.

**Prompt guidance:** The script should sound like a person talking, not a resume being read aloud. Use first person. Include natural transitions ("That's when I moved to..." not "Subsequently, I transitioned to..."). The 90-second version must actually be speakable in 90 seconds (~225 words). The 2-minute version ~450 words. Count them.

**Citations:** Each career beat sourced to `pf_bullet_bank` entry. Role mapping sourced to JD requirements (Section 1 output). Source type: `manual_entry` (bullets) + `job_board` (JD).

---

### Section 13: Likely Interview Questions

**Purpose:** Predict what they'll ask you, and have an answer ready.

**Inputs:** Base context + `pf_story_bank` + `pf_bullet_bank` + Section 1 (Role Decode) output

**Output structure:**
- **Behavioral Questions** (5-7) — Derived from JD requirements and company values. For each: the likely question, why they'd ask it (which requirement it probes), and a matched STAR story from your `pf_story_bank` (or "No matching story — consider preparing one about [topic]").
- **Technical / Product Questions** (3-5) — Domain-specific questions based on the role's product area. Case studies, estimation, system design, product strategy — whatever's appropriate for the level and function.
- **Role-Specific Curveballs** (2-3) — Questions unique to this role or company. "Why this company specifically?" "How would you handle [specific challenge from Section 9]?" "Tell me about a time you [specific JD requirement]."
- **Questions About Your Gaps** (1-3) — The questions you're dreading — the ones that probe your red/yellow items from Section 7. For each: the likely question, and a prepared answer that's honest but well-positioned.
- **Your Answer Framework** — For behavioral questions, a reminder of your STAR structure with the specific stories selected. For technical questions, your go-to frameworks.

**Prompt guidance:** Questions should be specific to this role, not generic PM/engineering interview questions. "Tell me about a time you influenced without authority" is good *only if* the JD signals cross-functional leadership. Every question needs a "why they'd ask this" rationale tied to the JD or company context. If `pf_story_bank` is empty, flag it and suggest story topics to prepare.

**Citations:** Each question sourced to the JD requirement or company value it probes. Matched stories sourced to `pf_story_bank`. Source type: `manual_entry` (stories) + `job_board` (JD requirements) + `ai_generated` (question prediction).

---

### 3.1 Degraded Mode (Opaque Recruiter Outreach)

> **Status: Planned** — Not yet implemented. Spec retained for future development.

This section describes how the system will handle roles with partial information (company-unknown, role-unknown, or both).

When a role is tracked with partial information — unknown company, unknown role, or both (see main PRD Section 7.1.7) — the Research Brief operates in degraded mode. Each section either generates normally, generates with reduced scope, or is blocked entirely with a message explaining what's needed to unlock it.

#### Company-Unknown Mode (`confidential.company = true`)

The company name is not disclosed. The user may have hints (industry, funding stage, headcount range) stored in `company.knownAttributes`.

| # | Section | Status | Behavior |
|---|---------|--------|----------|
| 1 | Role Decode | ✅ Available | Generates from JD text. Company name replaced with "[Target Company]". |
| 2 | Company Now | ❌ Blocked | "Company not yet disclosed. This section unlocks when you reveal the company." |
| 3 | Funding & Corporate | ❌ Blocked | Same. If `knownAttributes` has funding hints, shows them as "What you know so far: Series D, 200-500 employees." |
| 4 | Competitive Landscape | ⚠ Partial | If JD describes the product area, generates landscape for that area without naming the specific company. "Companies in the [AI developer tools] space include..." |
| 5 | Team & Org Intelligence | ⚠ Partial | Interviewer research works if names provided. Org structure blocked. |
| 6 | Network & Connections | ❌ Blocked | Can't cross-reference without company name. Shows: "Reveal the company to see your connections there." |
| 7 | Fit Analysis | ✅ Available | Maps your bullets against JD requirements — doesn't need company name. |
| 8 | Comp Intelligence | ⚠ Partial | Can estimate range from level + industry + location hints. Less precise without company name. Flagged as approximate. |
| 9 | Strategic Challenges | ⚠ Partial | Can infer from JD language and industry hints. Generic challenges clearly labeled as such. |
| 10 | Culture & Values | ❌ Blocked | "Company not yet disclosed. This section unlocks when you reveal the company." |
| 11 | Questions to Ask | ⚠ Partial | Generates role-specific questions (from JD) but not company-specific ones. Adds: "Once the company is revealed, this section will add company-specific questions." |
| 12 | TMAY Script | ✅ Available | Built from your resume positioned for the role. Uses "[Target Company]" as placeholder. |
| 13 | Likely Interview Qs | ✅ Available | Behavioral + technical derived from JD. Doesn't need company name. |

**Summary:** 5 sections fully available, 4 partial, 4 blocked. The user can still meaningfully prepare for the role.

#### Role-Unknown Mode (`confidential.role = true`)

The company is known but the specific role/JD hasn't been shared. The user may have hints from recruiter conversations stored in `role.roleHints` and `role.knownContext[]`.

**Key behavior:** Section 0 (Known Context) activates as the JD substitute. It aggregates all hints and recruiter intel into a "Reconstructed Role Profile" that downstream sections use where they'd normally use JD text. Every hint-derived claim is labeled: "⚠ Based on recruiter intel, not confirmed JD."

| # | Section | Status | Behavior |
|---|---------|--------|----------|
| 0 | Known Context | ✅ **Activates** | Aggregates roleHints + knownContext log into structured format. Generates "Reconstructed Role Profile" paragraph. Shows what's still unknown. This is the anchor when JD is absent. |
| 1 | Role Decode | ⚠ Partial | Generates from Reconstructed Role Profile instead of JD. Less precise but still covers: inferred requirements from hints, level signals from recruiter language, watch items. Clearly labeled as recruiter-intel-based. |
| 2 | Company Now | ✅ Available | Full company research — doesn't need role specifics. |
| 3 | Funding & Corporate | ✅ Available | Full funding and structure research. |
| 4 | Competitive Landscape | ⚠ Partial | Can research company-level competition. If `roleHints.productArea` exists, scopes to that area. Otherwise company-wide. |
| 5 | Team & Org Intelligence | ⚠ Partial | General org research available. If `roleHints.reportingLine` or `roleHints.teamSize` exist, uses them. Interviewer research works if names provided. |
| 6 | Network & Connections | ✅ Available | Full cross-reference against company name. If `roleHints.productArea` exists, prioritizes connections in that area. |
| 7 | Fit Analysis | ⚠ Partial | Maps your bullets against the Reconstructed Role Profile's inferred requirements. Green/yellow/red still works but coverage is less complete. Prominently flagged: "This analysis is based on recruiter intel. It will be more precise with the full JD." |
| 8 | Comp Intelligence | ⚠ Partial | Uses company name + `roleHints.level` + `roleHints.function`. More precise than company-unknown mode but less than full-JD mode. |
| 9 | Strategic Challenges | ⚠ Partial | Company-level challenges fully available. First 90 days generated from Reconstructed Role Profile — less specific but still useful. |
| 10 | Culture & Values | ✅ Available | Full culture research — doesn't need role specifics. |
| 11 | Questions to Ask | ⚠ Partial | Company-specific questions available. Role-specific questions generated from hints — focuses on confirming/clarifying the unknowns. Includes: "Questions to ask the recruiter to fill in gaps." |
| 12 | TMAY Script | ⚠ Partial | Generates from your resume positioned toward `roleHints.function` + `roleHints.level` + `roleHints.productArea`. Less tailored than with a real JD but still role-directional. Labeled: "Draft — refine when JD is available." |
| 13 | Likely Interview Qs | ⚠ Partial | Generates from `roleHints.function` + `roleHints.level` + company context. Behavioral questions based on function norms, technical questions based on product area hints. Matched STAR stories still work. |

**Summary:** 5 sections fully available, 8 partial, 0 blocked. With hints and known context, *nothing is completely blocked* — every section generates something useful. The quality improves as more intel arrives.

#### Both-Unknown Mode (`confidential.company = true, confidential.role = true`)

Minimal information — maybe just a recruiter's vague description. But even here, Known Context activates.

| # | Section | Status | Behavior |
|---|---------|--------|----------|
| 0 | Known Context | ✅ **Activates** | Shows whatever is known — even if it's just "recruiter from Greylock, AI space, Series D." |
| 1 | Role Decode | ⚠ Partial (if hints) / ❌ Blocked (if none) | If `roleHints` has function + level, generates a minimal analysis. Otherwise blocked. |
| 2 | Company Now | ❌ Blocked | "Company not yet disclosed." |
| 3 | Funding & Corporate | ❌ Blocked | Shows `knownAttributes` if any: "What you know: Series D, 200-500 employees." |
| 4 | Competitive Landscape | ❌ Blocked | If `knownAttributes.industry` exists, can generate a broad landscape for that industry. Otherwise blocked. |
| 5 | Team & Org | ❌ Blocked | Interviewer research works if names provided. Everything else blocked. |
| 6 | Network & Connections | ❌ Blocked | Can't cross-reference without company name. |
| 7 | Fit Analysis | ⚠ Partial (if hints) / ❌ Blocked (if none) | If `roleHints` exist, generates rough mapping. Otherwise blocked. |
| 8 | Comp Intelligence | ⚠ Partial | Very rough estimate from level + industry hints. |
| 9 | Strategic Challenges | ❌ Blocked | |
| 10 | Culture & Values | ❌ Blocked | |
| 11 | Questions to Ask | ⚠ Partial | Discovery questions to ask the recruiter: "What's the company? What team? What level? What does the interview process look like?" |
| 12 | TMAY Script | ⚠ Partial (if hints) / ❌ Blocked (if none) | If function + level hints exist, generates a generic version for that function. |
| 13 | Likely Interview Qs | ⚠ Partial (if hints) / ❌ Blocked (if none) | If function + level hints exist, generates standard questions for that role type. |

**Summary:** With hints: 1 full + 6 partial + 6 blocked. Without hints: 1 full + 3 partial + 9 blocked. The brief focuses on helping you figure out what to ask to learn more.

#### Unlock Roadmap UI

When any sections are blocked or partial, the brief shows an **Unlock Roadmap** banner at the top:

```
┌────────────────────────────────────────────────────────────────┐
│  📋 Brief Status: 5 full / 8 partial / 0 blocked              │
│                                                                │
│  To improve partial sections:                                  │
│  → Add the full JD         →  upgrades 8 sections from        │
│    partial to full (Role Decode, Fit Analysis, TMAY, etc.)     │
│  → Reveal company name     →  unlocks Company Now, Funding,   │
│    Culture, Network                                            │
│  → Add more recruiter intel →  strengthens Known Context,      │
│    which feeds all partial sections                            │
│  → Add interviewer names   →  unlocks Interviewer Profiles     │
│  → Add resume bullets      →  strengthens Fit Analysis, TMAY  │
│                                                                │
│  [Reveal Company]  [Reveal Role]  [Log Recruiter Intel]        │
│  [Add in Pipeline →]                                           │
└────────────────────────────────────────────────────────────────┘
```

The **"Log Recruiter Intel"** button opens an inline form to add a new `knownContext` entry — date, channel (call/email/LinkedIn/text), and freeform notes. Each entry is timestamped and immediately feeds into Section 0, which re-synthesizes the Reconstructed Role Profile and cascades to downstream sections.

The "Reveal Company" and "Reveal Role" buttons link back to the Pipeline detail panel's reveal actions (see main PRD Section 7.1.7). When a reveal happens, affected sections auto-invalidate and show "New data available — click Refresh to regenerate."

---

## 4. MCP Tool: `pf_generate_brief_section`

> **Status: Planned** — Not yet implemented. The browser currently calls Claude directly without MCP. This spec is retained for future server-side generation if needed.

This section describes an optional MCP tool interface that could be used for server-side generation.

### Tool Specification

```
Tool: pf_generate_brief_section
Description: Generate one section of a Research Brief using Claude.
             Reads role/company data from localStorage (via bridge) and
             artifacts from MCP storage. Returns generated HTML content
             with inline citation markers.

Input Schema:
  roleId       (string, required)  — Role ID from pf_roles
  companyName   (string, required)  — Company name from pf_companies
  sectionNum   (integer, required)  — Section number (1-13)
  forceRefresh (boolean, optional)  — Ignore cached version, regenerate. Default: false

Output:
  {
    sectionNum: number,
    title: string,
    content: string,          // HTML content with [n] citation markers
    citations: Citation[],    // Array of citation records (saved to MCP)
    artifactId: string,       // Artifact ID of saved section
    generatedAt: string,      // ISO timestamp
    inputsUsed: string[],     // Which data sources were available
    inputsMissing: string[]   // Which data sources were missing
  }
```

### Prompt Construction

Each section call builds a prompt with three parts:

**System prompt (shared across all sections):**
```
You are a job search preparation researcher. You generate highly specific,
role-tailored research briefs for interview preparation. Every claim must
be sourced. Never fabricate company data, funding amounts, names, or
metrics. If you don't have specific data, say so clearly and suggest
where the user can find it.

Your output is HTML. Use semantic markup (h3, p, ul, table, strong, em).
Include citation markers as [n] where n maps to the citations array
you return alongside the content.

Be direct. No filler. No "in today's rapidly evolving landscape."
Every sentence should give the reader something they can use in an
interview or a decision.
```

**Context block (shared, injected per call):**
```
<role>
  Title: {role.title}
  Company: {company.name}
  Level: {role.targetLevel}
  Positioning: {role.positioning}
  Stage: {role.stage}
  URL: {role.url}
  Date Added: {role.dateAdded}
</role>

<company>
  Domain: {company.domain}
  Type: {company.companyType}
  Funding: {company.fundingStage}
  Headcount: {company.headcount}
  Mission: {company.missionStatement}
  Remote: {company.remotePolicy}
  Tech Stack: {company.techStack}
</company>

<jd>
{role.jdText}
</jd>

<!-- Included only for sections that need it: -->
<bullet_bank>
{JSON.stringify(pf_bullet_bank)}
</bullet_bank>

<story_bank>
{JSON.stringify(pf_story_bank)}
</story_bank>

<connections>
{JSON.stringify(pf_connections)}
</connections>

<comp_data>
{JSON.stringify(pf_comp_cache[companyKey])}
</comp_data>
```

**Section-specific user prompt:** Each section has a dedicated prompt matching the output structure defined in Section 3 above. The prompt includes the exact output format expected and any section-specific constraints.

### Generation Order & Dependencies

| Section | Depends On | Can Parallel? |
|---------|-----------|---------------|
| 0. Known Context | None (only runs when JD absent) | ⏳ Pre-batch (if needed) |
| 1. Role Decode | Section 0 (if no JD) or None (if JD exists) | ✅ First batch |
| 2. Company Now | None | ✅ First batch |
| 3. Funding & Corporate Structure | None | ✅ First batch |
| 4. Competitive Landscape | None | ✅ First batch |
| 5. Team & Org Intelligence | None | ✅ First batch |
| 6. Network & Connections | None | ✅ First batch |
| 7. Fit Analysis | Section 1 (requirements list) | ⏳ Second batch |
| 8. Compensation Intelligence | None | ✅ First batch |
| 9. Strategic Challenges & First 90 Days | Section 1 + 2 + 4 | ⏳ Second batch |
| 10. Culture & Values Decode | None | ✅ First batch |
| 11. Questions to Ask | Sections 1-10 | ⏳ Third batch (runs last) |
| 12. TMAY Script | Section 1 (role decode) | ⏳ Second batch |
| 13. Likely Interview Questions | Section 1 + 7 (fit analysis) | ⏳ Second batch |

**Batch execution:**
- **Pre-batch** (if JD absent): Section 0 — assembles Known Context and Reconstructed Role Profile
- **Batch 1:** Sections 1, 2, 3, 4, 5, 6, 8, 10 — all independent, run in parallel
- **Batch 2:** Sections 7, 9, 12, 13 — depend on Batch 1 outputs
- **Batch 3:** Section 11 — depends on all other sections

---

## 5. Caching & Invalidation

### Cache Strategy

Each section is cached as an artifact in the MCP server (type: `research_brief`, tagged with `{company, roleId, sectionNum}`). The browser module reads from these artifacts.

### Automatic Invalidation Triggers

> **Status: Implemented (v3.13.0)** — Checks pf_brief_invalidation on load, displays banner for stale sections, provides "Regenerate Stale Sections" button.

The system will monitor for data changes and invalidate cached sections:

| Trigger | Sections Invalidated | Reason |
|---------|---------------------|--------|
| JD text changes | All 13 | JD is the anchor — everything depends on it |
| Role title changes | 1, 5, 7, 9, 12 | Level signals and fit mapping change |
| Positioning changes (IC ↔ management) | 1, 5, 7, 9, 12, 13 | Fundamentally different prep |
| Company profile updated | 2, 3, 4, 10 | Company-centric sections need refresh |
| Interviewer names added/changed | 5 | Interviewer profiles section |
| Bullet bank updated | 7, 12, 13 | Fit analysis and scripts use bullets |
| Story bank updated | 13 | Interview questions match stories |
| Connections updated | 6 | Network cross-reference changes |
| Comp cache updated | 8 | Compensation data refreshed |
| Stage changes (e.g., Applied → Screen) | 11 | Questions to ask shift by interview phase |

When a trigger fires, affected sections will be marked `stale` and the browser will show a "⚠ Stale — data has changed since this was generated" badge with a "Refresh" button.

### Manual Controls

- **Refresh single section** — Regenerate one section, preserving others
- **Refresh all** — Regenerate all 13 sections from scratch
- **Clear brief** — Delete all cached sections for this role (soft delete in MCP)

---

## 6. Browser UI

### Layout (unchanged from v1 shell)

- **Left sidebar:** Role selector, company card, role details, JD text, interviewer names input, action buttons
- **Main area:** Sections rendered as cards with status badges, content, citation markers, refresh buttons
- **Right sidebar:** Table of contents (sections 1-13), progress indicator

### New UI Elements for v2

**Shipped:**
- **Generation status** — "Generating...", "Fresh" (with timestamp), and "Error" badges
- **Section refresh button** — Per-section "Refresh" button to regenerate a single section
- **Missing data callouts** — Yellow banner within a section: "⚠ This section would be stronger with your resume bullets. Add them in Resume Tailor → Bullet Bank."

**Status: Implemented (v2.2.0)** — The following UI elements are now live:
- **Citation markers** `[n]` — Clickable popovers showing: claim text, source type badge, source link, fetch date, trust level. (Feature 7)
- **Section status badges (extended)** — Pills showing Fresh/Cached/Stale/Error/Generating states per section. (Feature 8)
- **Generation progress bar** — Real-time progress indicator showing "Generating section 3/13..." with visual feedback. (Feature 9)

### Keyboard Shortcuts

**Shipped:**
| Key | Action |
|-----|--------|
| 0-9 | Jump to sections 0-9 |
| Shift+0 to Shift+3 | Jump to sections 10-13 |

**Implemented (v2.2.0):**
| Key | Action |
|-----|--------|
| G | Generate full brief |

**Status: Planned** — The following keyboard shortcuts are specified but not yet implemented:
| Key | Action |
|-----|--------|
| R | Refresh current section |
| P | Print brief |
| E | Export as HTML artifact |
| [ / ] | Collapse/expand sidebar |
| C | Toggle all citations (expand/collapse) |

---

## 7. Export & Artifacts

### Auto-Save & PDF Storage

**Shipped:**
When all sections are generated, the brief is automatically:
- Saved to localStorage with keys: `research_{companySlug}_{roleId}_{sectionNum}`
- Compiled into a PDF via `html2pdf.js` and stored in IndexedDB
- Attached to the pipeline role's `artifacts[]` array for discovery from the Pipeline module

**Status: Planned** — MCP-based artifact persistence:
The system will also save the brief to the MCP server as a `research_brief` artifact:
```
Type: research_brief
Filename: research-brief_{company}_{roleTitle}_{date}.html
Tags: [company, roleId, "complete", date]
Metadata: { sectionsGenerated: 13, inputsMissing: [...], generatedAt: ISO }
```

Previous versions will never be overwritten — each generation creates a new artifact, supporting version comparison: "How did my prep evolve between the recruiter screen and the onsite?"

### Export Formats

**Shipped:**
- **PDF** — auto-generated and stored in IndexedDB; attached to pipeline role

**Status: Implemented (v3.13.0):**
- **HTML export** — Standalone file with embedded CSS, all sections, citations, and source links
- **Markdown export** — For pasting into notes apps or sharing with mentors
- **Export dropdown menu** — Quick access to all export formats

**Status: Planned:**
- **Print** — Browser print dialog with print-optimized CSS (no sidebars, clean headers)

---

## 8. Integration Points

| Module | Integration | Direction |
|--------|------------|-----------|
| **Pipeline Tracker** | Reads role + company data, JD text | Brief ← Pipeline |
| **Pipeline Tracker** | Invalidation triggers on data changes | Pipeline → Brief |
| **Resume Tailor** | Reads bullet bank for Fit Analysis, TMAY | Brief ← Resume |
| **Mock Interview** | Reads story bank for Interview Questions | Brief ← Mock |
| **Comp Intelligence** | Reads comp cache for Compensation section | Brief ← Comp |
| **Outreach Agent** | Reads Network section for intro suggestions | Outreach ← Brief |
| **Debrief Agent** | Reads Questions section to track what was actually asked | Debrief ← Brief |
| **Dashboard** | Brief completion status feeds pipeline health metrics | Dashboard ← Brief |
| **Citations (7.12)** | All citations saved via pf_save_citation | Brief → Citations |
| **Source Ledger** | Brief citations appear in centralized roll-up | Ledger ← Brief |
| **Artifacts MCP** | All section content saved as artifacts | Brief → MCP |

---

## 9. Implementation Roadmap

> **Status: Historical Reference** — These phases describe the development path taken. Most core features (Phases 1-2) are complete in browser-first form. Phases 3-4 represent ongoing or future work.

### Phase 1: Core Generation Engine (Complete — Browser-First)

**Shipped:**
- Browser-side Claude API calls via `generateSectionViaAPI()`
- Prompt construction with context injection for all 13 sections
- All section generation (Batches 1, 2, 3 sequential; parallel within batch)
- localStorage caching with keys: `research_{companySlug}_{roleId}_{sectionNum}`
- Citation parsing from `<citations>` JSON blocks
- Section rendering with status badges ("Fresh", "Error", etc.)
- "Generate Brief" button wired to `generateBrief()` function

**Not Shipped (originally planned for MCP):**
- MCP `pf_generate_brief_section` tool
- MCP artifacts for brief storage (using localStorage + IndexedDB instead)
- `pf_save_citation` tool (citations stored locally)

### Phase 2: Smart Caching & Invalidation (Partial)

**Shipped:**
- Per-section refresh capability via `refreshSection()`
- "Missing data" callouts showing inputs not available
- Auto-save to localStorage
- PDF generation and IndexedDB storage
- Auto-generation on first visit to a role

**Status: Planned:**
- Invalidation trigger detection (JD changes, profile updates, etc.)
- Stale/fresh/degraded badges beyond "Fresh"
- Cross-module invalidation (Pipeline edits trigger brief staleness)

### Phase 3: Deep Integrations (Partial)

**Shipped:**
- Bullet bank read for Sections 7, 12, 13
- Story bank read for Section 13 (STAR matching)
- Connections cross-reference for Section 6
- Comp cache read for Section 8
- Interviewer names read for Section 5

**Status: Planned:**
- Interviewer name research (LinkedIn profile fetch)
- Cross-module invalidation signals

### Phase 4: Polish (Partial)

**Shipped:**
- Section keyboard shortcuts (0-9, Shift+0-3)
- Sidebar collapse toggle
- Generation progress bar (batch progress) (v2.2.0)
- Citation popovers with source links (v2.2.0)
- G keyboard shortcut to trigger generate (v2.2.0)

**Status: Planned:**
- Export to Markdown and HTML
- Brief version comparison (diff two briefs for the same role)
- Keyboard shortcuts for citation navigation (C to toggle, etc.)

---

## 10. Success Criteria

A v2 Research Brief is successful when:

1. **Every section names the company, the role, and at least one specific fact** — no generic output that could apply to any company.
2. **Fit Analysis maps your actual bullets against JD requirements** — not platitudes.
3. **Competitive Landscape names real competitors** — not "other companies in the space."
4. **Every factual claim has a citation** — with source type, link, and date.
5. **The brief is useful enough that you'd actually read it before an interview** — the bar is: does this save you 2+ hours of manual research?
6. **Missing data is called out, not worked around** — if we don't have your resume bullets, we say so.

---

## 11. Success Metrics

- Citation accuracy: ≥95% of factual claims must be sourceable to JD, enriched company data, or user input. Unsourceable claims must be flagged as "inferred."
- Section completeness: all 14 sections generated in <60 seconds total. If any section fails, show partial brief with clear "Section unavailable" placeholder.
- Staleness detection: if company data is >30 days old, show subtle indicator ("Data last refreshed X days ago"). If >90 days, show warning.
- Cache hit rate: target ≥70% of brief views served from cache (measures efficiency of caching strategy).
- User satisfaction proxy: track regeneration rate — if users regenerate >40% of briefs immediately, prompts need tuning.

---

## 12. Testing Strategy

- Golden test cases: maintain 10 hand-curated roles (mix of FAANG, growth-stage, early-stage, unusual industries) with known-good brief outputs in `docs/eval/research-brief/`.
- Regression protocol: before any prompt change, generate briefs for all 10 golden cases. Compare section-by-section against prior output. Flag: factual changes, tone shifts, missing sections, new hallucinations.
- Automated checks: validate that every `<citation>` tag resolves to a real data source. Check section headers match expected 14. Check company name appears in first 100 words of company section.
- Freshness validation: for each golden case, verify cited URLs are live (HTTP 200).
- Edge cases to test: role with no JD text, company with no enrichment data, role at company with zero LinkedIn connections, extremely long JD (>5000 words).

---

## 13. Risk / Failure Modes / Guardrails

- **Company confusion** (High): Brief mixes up Company A and Company B data. Mitigation: system prompt declares `<company>` tag as authoritative truth; first 500 chars of JD validated against company name; mismatch triggers warning banner.
- **Stale data cited as current** (Medium): Brief cites funding round from 2 years ago as "recent." Mitigation: date-stamp all enrichment data; show freshness indicator; brief intro states "Based on data as of {date}."
- **Generation timeout** (Medium): Claude API takes >60s or errors out. Mitigation: show progress bar per section; allow cancel; serve cached brief if available; retry once with shorter prompt.
- **JD mismatch** (Medium): Enriched JD doesn't match the actual role (wrong company). Mitigation: `getCompanyDomain()` validates JD source; mismatch guard prepends warning; system prompt ignores mismatched company references.
- **API key invalid/exhausted** (Low): Mitigation: clear error message with link to settings; don't retry on 401/403.
